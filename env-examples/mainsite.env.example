# Mainsite Environment Variables
# Copy this file to mainsite/.env.production
# IMPORTANT: Use the SAME domain as your main site (nginx proxies /api to backend).

# Backend API URL (production - main site proxies /api)
VITE_API_URL=https://krushnasadasahayte.org/api

# If users will use www, you can use www URL instead:
# VITE_API_URL=https://www.krushnasadasahayte.org/api

# Do NOT use api.kss.org or other domains that don't resolve in production.
