# Frontend (Admin Panel) Environment Variables
# Copy this file to frontend/.env.production
# IMPORTANT: Use the SAME domain as your admin site (nginx proxies /api to backend).

# Backend API URL (production - admin site proxies /api)
VITE_API_URL=https://admin.krushnasadasahayte.org/api

# Do NOT use api.kss.org or api.krushnasadasahayte.org unless you set up that subdomain + DNS + SSL.
