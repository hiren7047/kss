# Main Site - krushnasadasahayte.org (PUBLIC WEBSITE)
# default_server = IP access and unknown Host get mainsite, not admin
# Use this BEFORE SSL. Run: sudo certbot --nginx -d krushnasadasahayte.org -d www.krushnasadasahayte.org
# App root: /var/www/kss/kss (git clone creates kss/ subfolder)

server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name krushnasadasahayte.org www.krushnasadasahayte.org;

    root /var/www/kss/kss/mainsite/dist;
    index index.html;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/json application/javascript;

    location / {
        try_files $uri $uri/ /index.html;
    }
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # API proxy (mainsite calls same-origin /api)
    location /api {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }

    location /uploads {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    access_log /var/log/nginx/krushnasadasahayte.org.access.log;
    error_log /var/log/nginx/krushnasadasahayte.org.error.log;
    client_max_body_size 10M;
}
